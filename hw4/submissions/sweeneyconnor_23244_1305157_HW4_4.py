# -*- coding: utf-8 -*-
"""
HW4 #4:
Find the root of the following functions using the Secant method:
    a. f1(x) = -x**5 + (1/3)*x**2 + 1/2, with -10 < x < 10
    b. f2(x) = cos**2(x) + 0.1*( x +5), with -10 < x < 10
    c. f3(x) = sin( x/3) + 0.1*( x +5), with -3 < x < 3
"""
#====================================================================================
            #Imports
#====================================================================================
import numpy as np
import opt_utils as ou



#====================================================================================
            #Variables, functions
#====================================================================================

x1min = -10

x2min = -3

def f1(x):
    return (-x**5) + ((1/3)*x**2) + (1/2)
def f2(x): 
    return np.cos(x)**2 + 0.1
def f3(x): 
    return np.sin( x/3) + 0.1*( x +5)



#for x in range(x1min, x1max):
   # ou.my_Secant(f1, x1min, x1min + 0.1, tol=1e-4, N=20)
   # x += 1
#=====================================================================================
            #Find, print roots
#=====================================================================================

print ou.my_Secant(f1, x1min, x1min + 0.1, tol=1e-6, N=40), '<--------Root for a.'
print ou.my_Secant(f2, x1min, x1min + 0.1, tol=1e-6, N=20), '<--------Root for b.'
print ou.my_Secant(f3, x2min, x2min + 0.1, tol=1e-6, N=20), '<--------Root for c'


#=====================================================================================
"""

Answers:
    a.) -0.057301
    b.) No roots
    c.) -1.177


"""    